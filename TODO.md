# Fix up selenium-webdriver-at-spi itself
- The below is a blocker for a lot of selenium testing
https://invent.kde.org/sdk/selenium-webdriver-at-spi/-/merge_requests/69
- https://invent.kde.org/sdk/selenium-webdriver-at-spi/-/merge_requests/112 also this

# (Re)implement tests with selenium
- ~~calamares installation~~ done
- disable_screen_dim_and_screen_off - this needs upstream changes see https://invent.kde.org/sdk/selenium-webdriver-at-spi/-/merge_requests/69
- disable_screen_lock - this needs the same upstream changes
- configure_automatic_login - this probably also needs the same upstream changes (note: should these tests just be done by writing directly to the rc files?)
- panel tests, - system tray, digital clock
- desktop tests - create file, switch windows
- KISS test, difficult. - partway completed, selenium logic is done but need to work out how to actually *run* the test since there's no log-in-able users when it's running
- Tests for discover and firefox (test plasma integration in firefox?)

# Fix tests so they work properly
- ~~Shutdown and reboot, these shouldn't be hardware controls~~ done

# Get full testing flows working
- Get a working live -> installation -> KISS -> sanity test 
- Get a working previous live -> installation -> KISS -> upgrade -> sanity test

# Fix stinky smelly code
- ~~Work out how to be able to log in to a variety of users with different passwords~~ done
- Strip out code that's probably going to be redundant seeing we aren't using needles
- Address README.md notes at the bottom
- ~~maybe wrap up selenium tests in some clean functions for ease of development, rather than just launching them through arbitrary commandline on the OpenQA side?~~ done
- ~~flesh out some helpers so we can capture assets generated by selenium tests to upload to OpenQA, and maybe have some helpers so multiple OpenQA tests can be grouped in one selenium test so we aren't spinning it up over and over again to test different functionality in one piece of software. This would need to spit out JUnit XML~~ done

# Look at how GnomeOS does things
- so I don't have to reinvent the wheel
- and so I have some inspiration

# CI, pipelines
Thoughts:
- Have a shared worker entrypoint script.
- Utilise the openqa_worker podman container.
- Have a docker compose file for local mocking, and a Gitlab CI stage which is triggered from KDE Linux for testing on the server.
- They both use this worker entrypoint script to bootstrap.
- Have a job submission script that's called separately. This is called in the next gitlab CI stage, or called locally whenever in the worker.
- This then polls OpenQA until success or failure of the test suite. In CI this determines the outcome.
- Would be good to preserve the ability to run a single-instance server.
- Include an .env file for the openqa server and API keys, include an .env.example with dummy values

# Bootstrap with sysext
from some cursory probing:
- the repo can have a sysext directory in it that contains a bootstrapping service
- this bootstrapping service can unlock root and set everything up blahblahblah. you can put whatever you want in it 
- use `systemd-dissect` to install the sysext on the live image before the image is submitted to openqa for testing
- then do some finangling with `calamares@subvol` - check if the injected test sysext exists and copy it into the image if it does
- then the serial terminal can be perpetually logged in as root and we don't have to do fragile user management, and we can also test plasma-login/plasma-setup with selenium and do whatever else we want
- in future we can build the test sysext with the image so we don't have to ship selenium-webdriver-at-spi and deps with it, but that's for later.
*or*
- use `fw_cfg` to set a root password that's read by systemd from the firmware. doesn't really have the same security issues as providing a permanently unlocked root cmdline would in the base system, but it's not a very extensible solution
