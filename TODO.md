# Fix up selenium-webdriver-at-spi itself
- The below is a blocker for a lot of selenium testing
https://invent.kde.org/sdk/selenium-webdriver-at-spi/-/merge_requests/69
- https://invent.kde.org/sdk/selenium-webdriver-at-spi/-/merge_requests/112 also this

# (Re)implement tests with selenium

- calamares installation (done)
- disable_screen_dim_and_screen_off - this needs upstream changes see https://invent.kde.org/sdk/selenium-webdriver-at-spi/-/merge_requests/69
- disable_screen_lock - this needs the same upstream changes
- configure_automatic_login - this probably also needs the same upstream changes (note: should these tests just be done by writing directly to the rc files?)
- panel tests, - system tray, digital clock
- desktop tests - create file, switch windows
- KISS test, might be difficult. Could log in as whatever user it is, get its environment from /proc and sniff all the variables, then pass in its pid to the test? Then ideally check if the marker it spits out is on the disk.
- Tests for discover and firefox (test plasma integration in firefox?)

# Fix tests so they work properly
- Shutdown and reboot, these shouldn't be hardware controls

# Get full testing flows working
- Get a working live -> installation -> KISS -> sanity test 
- Get a working previous live -> installation -> KISS -> upgrade -> sanity test
- Think about how to get this to work in kde-linux CI while we wait on a server, and in such a way that it's easy to just swap out the functionality once we do have a server. Probably just a matter of cloning this repo in a CI runner and having a script in here that does everything and reports back

# Fix stinky smelly code
- Work out how to be able to log in to a variety of users with different passwords
- Strip out code that's probably going to be redundant seeing we aren't using needles
- Address README.md notes at the bottom
- maybe wrap up selenium tests in some clean functions for ease of development, rather than just launching them through arbitrary commandline on the OpenQA side?
- flesh out some helpers so we can capture assets generated by selenium tests to upload to OpenQA, and maybe have some helpers so multiple OpenQA tests can be grouped in one selenium test so we aren't spinning it up over and over again to test different functionality in one piece of software. This would need to spit out JUnit XML

# Look at how Fedora and OpenSUSE do things
- so I don't have to reinvent the wheel
- and so I have some inspiration
